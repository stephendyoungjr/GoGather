{
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.7.2",
    "pg": "^8.13.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.5",
    "sequelize-cli": "^6.6.2"
  },
  "devDependencies": {
    "@babel/plugin-proposal-private-property-in-object": "^7.21.11"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && npm run sequelize db:migrate && npm run sequelize db:seed:all",
    "start": "cd backend && npm start"
  }
}
